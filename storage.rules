rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can view files (public avatars)
      allow read: if true;

      // Only logged-in users can upload/replace files
      allow write: if request.auth != null;
    }
  }
}
